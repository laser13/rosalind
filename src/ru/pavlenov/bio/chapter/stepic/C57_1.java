package ru.pavlenov.bio.chapter.stepic;

import org.apache.commons.lang.StringUtils;
import ru.pavlenov.bio.assemble.Euler;
import ru.pavlenov.bio.utils.Dump;
import ru.pavlenov.bio.utils.File;

import java.io.IOException;
import java.nio.charset.Charset;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.Collections;
import java.util.HashMap;

/**
 * Created by semen on 03.12.13.
 */
public class C57_1 {

    public static void start() throws IOException {

//        String input = File.readFile("/home/semen/IdeaProjects/bio/genome/data", Charset.defaultCharset());
//        String[] inputList = input.split("\n");
//
//        ArrayList<Euler.Vertex> nodeList = new ArrayList<>();
//
//        for (int i = 0; i < inputList.length; i++) {
//            String[] pair = inputList[i].split(" -> ");
//            String[] outStr = pair[1].split(",");
//            ArrayList<Integer> out = new ArrayList<>();
//            for (String anOutStr : outStr) {
//                out.add(Integer.parseInt(anOutStr));
//            }
//            int index = Integer.parseInt(pair[0]);
//            nodeList.add(new Euler.Vertex(index, out));
//        }
//
//        Collections.sort(nodeList, Euler.Vertex::replace);
//
//        Integer[] cycle = Euler.findFullCycle(nodeList, 0);
//
//        Dump.println("RESULT: ");
//        Dump.println(StringUtils.join(cycle, "->"));

    }

}
